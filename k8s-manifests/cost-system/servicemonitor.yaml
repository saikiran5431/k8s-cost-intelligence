apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: cost-exporter
  namespace: cost-system
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: cost-exporter
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
